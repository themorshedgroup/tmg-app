// Navy Edge shell logo lockup (99x62 displayed). Hosted on the app site, NOT
// embedded as a base64 data: URI: Gmail refuses to render data: images, so
// the embedded version showed an empty box in every email (fixed 2026-09-22).
// The file is email-logo.png at the repo root, served by GitHub Pages.
export const LOGO_SRC = "https://app.themorshedgroup.com/email-logo.png";
