{
  "name": "tmg-listings-crawler",
  "version": "1.0.0",
  "private": true,
  "description": "Twice-weekly crawl of Austin commercial listings from broker sites into Supabase (The Morshed Group)",
  "type": "module",
  "scripts": {
    "crawl": "node src/index.js",
    "crawl:fetch-only": "SKIP_BROWSER=1 node src/index.js",
    "import:costar": "node src/import-costar.js",
    "check": "node --check src/index.js && for f in src/lib/*.js src/scrapers/*.js; do node --check $f; done"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cheerio": "^1.0.0",
    "csv-parse": "^5.5.6",
    "playwright": "^1.46.0"
  }
}
